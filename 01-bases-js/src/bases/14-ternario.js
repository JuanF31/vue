let firstName;
let lastName = 'Hernandez';

//console.log( `${ firstName || 'No firstName'} ${ lastName || 'No lasteName'}` )

const isActive = false

let message = ( isActive ) ? 'Activo' : 'Inactivo'


console.log(message)